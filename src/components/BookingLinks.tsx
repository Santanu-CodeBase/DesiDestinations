// This component is no longer needed as booking links are now embedded in the search results
import React from 'react';

const BookingLinks: React.FC = () => {
  return null;
};

export default BookingLinks;